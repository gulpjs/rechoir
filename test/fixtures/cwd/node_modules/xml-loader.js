const fs = require('fs');
const convert = require('xml-js');
const options = { compact: true, nativeType: true };

require.extensions['.xml'] = function(module, filename) {
  const xml = fs.readFileSync(filename, 'utf8');
  module.exports = convert.xml2json(xml, options);
};
